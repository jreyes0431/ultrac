/// Todo model
class Todo {
  String name;
  String content;
  bool isCompleted;

  Todo({
    /// Constructor with default values
    this.name = '',
    this.content = '',
    this.isCompleted = false,
  });
}
